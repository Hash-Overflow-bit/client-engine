'use client';
import { useState } from 'react';
import { markFollowUpDeliverySentAction, prepareFollowUpDeliveryAction } from '../app/actions/delivery';
export function FollowUpDeliveryActions({ draftId, deliveryId, status }: Readonly<{draftId:string;deliveryId?:string;status?:string}>){const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');async function run(fn:()=>Promise<{ok:boolean;error?:string}>){setBusy(true);const r=await fn();setBusy(false);setMessage(r.ok?'Updated':r.error??'Unable to update delivery');}return <div className="mt-3 flex flex-wrap gap-2"><button disabled={busy||Boolean(deliveryId)} onClick={()=>void run(()=>prepareFollowUpDeliveryAction(draftId))} className="rounded-md bg-blue-700 px-3 py-2 text-xs font-semibold text-white disabled:opacity-50">Prepare Delivery</button>{deliveryId&&status!=='sent'&&<button disabled={busy} onClick={()=>void run(()=>markFollowUpDeliverySentAction(deliveryId))} className="rounded-md border border-slate-300 px-3 py-2 text-xs font-semibold disabled:opacity-50">Mark Sent</button>}{message&&<p role="status" className="basis-full text-xs text-slate-600">{message}</p>}</div>}
