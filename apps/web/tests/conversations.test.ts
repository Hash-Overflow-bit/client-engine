import { describe, expect, it } from 'vitest';
import { ManualInboundMessageSource, canFollowUp, hasInboundReply } from '@client-engine/conversations';
describe('conversation safety',()=>{it('normalizes manual replies and detects inbound state',async()=>{const value=await new ManualInboundMessageSource().normalize({workspaceId:'w',conversationId:'c',leadId:'l',body:'Thanks',provider:'manual'});expect(value.body).toBe('Thanks');expect(hasInboundReply([{direction:'outbound'},{direction:'inbound'}])).toBe(true);});it('suppresses follow-up after reply or do-not-contact',()=>{const base={workspaceMatches:true,hasInboundReply:false,doNotContact:false,unsubscribed:false,leadStage:'contacted',deliverySent:true,outboundAllowed:true};expect(canFollowUp(base)).toBe(true);expect(canFollowUp({...base,hasInboundReply:true})).toBe(false);expect(canFollowUp({...base,doNotContact:true})).toBe(false);expect(canFollowUp({...base,deliverySent:false})).toBe(false);});});
